import java.io.*;

File appFile = new File(basedir, "app/target/app-1.0-SNAPSHOT.jar");
if (!appFile.exists() || !appFile.isFile()) {
    throw new FileNotFoundException("'app': Could not find generated JAR: " + appFile);
}

File app2File = new File(basedir, "app-includeCompileClasspath/target/app-includeCompileClasspath-1.0-SNAPSHOT.jar");
if (!app2File.exists() || !app2File.isFile()) {
    throw new FileNotFoundException("'app-includeCompilerClasspath': Could not find generated JAR: " + app2File);
}